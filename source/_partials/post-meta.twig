{% if page.tags %}
    {% set postMeta = page.tags %}
    {% set blogDate = page.date %}
{% else %}
    {% set postMeta = post.meta.tags %}
    {% set blogDate = post.date %}
{% endif %}
<div class="post-meta">
    <div class="col-xs-6 col-sm-6 col-lg-8 posted">
        <time itemprop="datePublished" content="{{ blogDate|date('jS. F. Y') }}" datetime="{{ blogDate|date('jS. M. Y') }}">
            <span class="day">{{ blogDate|date('jS') }}</span>&nbsp;
            <span class="month">{{ blogDate|date('F') }}</span>&nbsp;
            <span class="year">{{ blogDate|date('Y') }}</span>
        </time>
    </div>
    {% if postMeta %}
        <div class="col-xs-6 col-lg-4 tags text-right">
            <i class="fa fa-tags"></i>
            {% for tag in postMeta %}
                <a href="{{ site.url }}/blog/tags/{{ tag|url_encode(true) }}">{{ tag }}</a>{% if not loop.last %}, {% endif %}
            {% endfor %}
        </div>
    {% endif %}
</div>
